# Test repository for gulp tasks

Just simple gulp tasks

## Tasks for:
* `Coffeescript` (concat, uglify)
* `Sass` (compress, concat, rename, autoprefixer)
* `Jade`

## Running / Building

* `gulp` (server)
* `gulp build` (building)
